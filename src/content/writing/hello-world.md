---
title: "Hello, world"
description: "A first post — testing the writing setup."
pubDate: 2026-04-27
draft: false
---

This is a placeholder post. It exists so the writing index has something to
render and the post template has a real piece of content to lay out.

## Why this exists

When the real first post goes up, this one can be deleted (or kept and rewritten —
no one is judging).

Inline `code` looks like this. Code blocks look like this:

```ts
function hello(name: string): string {
  return `Hello, ${name}.`;
}
```

> A blockquote, in case I want to be wistful about something.
